# 코드 구조 감사 — 2026-07-10

## 요약

범위: `apps/*` + `packages/*` 전체 · 검사 66/66개 파일 · 🔴 2건 🟡 5건 🔵 8건

전반적으로 컨벤션 준수 상태가 좋다 — `px-screen` 사용 0건, `../` 부모 탐색 import 0건,
`//` 블록 주석 0건, 불필요한 `"use client"` 사실상 0건(1건 의심), `_lib` 분리·네이밍 준수.
남은 문제는 **반복 마크업의 컴포넌트 미추출**, **죽은 코드**, **토큰 없는 미세 값의 표류** 세 갈래다.

## 🔴 구조 문제 (중복·잘못된 배치 — 방치할수록 비싸짐)

| #   | 항목                | 위치                                                                                                                       | 근거                                                                                                                                                                           | 권장 조치                                                           |
| --- | ------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| 1   | 뒤로가기 버튼 3중복 | `signup/page.tsx:17-23` · `me/edit/_components/EditTopBar.tsx:17-23` · `onboarding/_components/OnboardingTopBar.tsx:21-26` | 세 곳 마크업이 클래스 문자열까지 **바이트 동일** (`<Link aria-label="뒤로" className="text-icon -ml-1.5 grid size-9 place-items-center active:opacity-60">` + `ArrowLeftIcon`) | 공용 `app/_components/BackButton.tsx`(`href` prop) 추출 후 3곳 교체 |
| 2   | 기자 티어 배지 중복 | `reels/_components/ReelItem.tsx:87-95` vs `reels/_components/ReelDetailSheet.tsx:52-62`                                    | `T{tier}` 배지 + 기자명 + 시각 마크업이 거의 동일(시트 쪽은 `shrink-0`·조회수만 추가)                                                                                          | `reels/_components/ReporterBadge.tsx`(또는 기자 줄 컴포넌트) 추출   |

## 🟡 컨벤션 위반 · 죽은 코드 (확실)

| #   | 항목                          | 위치                                                                                                                                                       | 근거                                                                                                                                                               | 권장 조치                                                              |
| --- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| 3   | 죽은 아이콘 export 7개        | `app/_components/icons.tsx:59,68,74,81,304,310,329`                                                                                                        | `HeartIcon` `CommentIcon` `ShareIcon` `BookmarkIcon` `BackIcon` `ChevronRightIcon` `SettingsIcon` — 전 저장소 참조 0 (피그마 벡터판 `LikeIcon` 등으로 대체된 잔재) | 삭제                                                                   |
| 4   | `STAGE_META.toneClass` 미사용 | `app/_lib/constants.ts:29-36` · `(home)/_components/HotHeroCard.tsx:30`                                                                                    | 단계별 색을 정의해 두고 어디서도 안 읽음 — `HotHeroCard`는 `text-media-on/50`으로 별도 하드코딩(정의와 사용의 불일치)                                              | `toneClass`를 쓰게 하거나 필드 삭제 (둘 중 하나로 일치시킬 것)         |
| 5   | `text-[10px]` 임의 글자 크기  | `HotHeroCard.tsx:29` · `TabBar.tsx:90` · `TopBar.tsx:29`                                                                                                   | 글자는 토큰 유틸만 규칙 위반. `--text-micro`가 이미 10px로 정의돼 있는데 세 곳이 임의값으로 우회                                                                   | 기존 `text-micro`로 교체 (새 토큰 불필요)                              |
| 6   | 임의 px — 아바타 필드         | `me/edit/_components/AvatarField.tsx:11,15`                                                                                                                | `size-[84px]` `size-[34px]` `border-[3px]` — 스케일 유틸 아닌 임의값                                                                                               | 스케일 유틸(`size-21` 등) 또는 토큰으로 교체                           |
| 7   | 스크림 색 표류                | `HotHeroCard.tsx:20-23`(`rgba(6,8,14,…)`) · `TabBar.tsx:68-71`(`rgba(4,6,11,…)`) · `ReelItem.tsx:55,66`(`rgba(5,8,14,…)`) · `MediaThumb.tsx:29`(`#05070c`) | "테마 무관 고정값"으로 의도된 예외지만, **같은 의도의 스크림 앵커색이 4파일에서 4가지 값**으로 흩어짐 — 교차 비교로만 보이는 표류                                  | `theme.css`에 `--plk-scrim` 앵커색 토큰 1개 추가, 4곳이 같은 변수 참조 |

## 🔵 개선 제안 (판단 여지 있음)

| #   | 항목                                    | 위치                                                                                                          | 근거                                                                                                                                            | 권장 조치                                                             |
| --- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| 8   | 시맨틱 간격 토큰 미사용                 | `theme.css`의 `--spacing-gap` `--spacing-gap-lg` `--spacing-section` `--text-display`                         | 사용처 0. 스킬/문서는 `gap`·`gap-lg`·`section`을 쓰라고 하는데 실제 코드는 전부 숫자 유틸 — 토큰을 지울지, 코드가 토큰을 쓰게 할지 팀 결정 필요 | 방향 결정 후 일괄 정리 (문서·스킬도 함께 갱신)                        |
| 9   | 타이포 미세값 표류                      | `tracking-[-0.2px]`×3 · `tracking-[-0.4px]`×3 · `tracking-[1px]`×2 · `leading-[1.35]`×2 · `leading-[1.32]` 등 | 텍스트 크기는 토큰인데 자간·행간은 임의값이 화면마다 반복                                                                                       | 타이포 토큰에 자간/행간을 포함하는 방향 검토                          |
| 10  | 팀 선택 로직 중복                       | `me/edit/_components/TeamPicker.tsx` vs `onboarding/team/_components/TeamSelectGrid.tsx`                      | `useState<TeamCode>` + `TEAM_ORDER.map` + active 분기 로직 동일 (UI는 pill vs 카드로 명확히 다름)                                               | 마크업은 유지, 선택 로직만 `useTeamSelection` 훅으로 공유 검토        |
| 11  | TopBar 껍데기 3종 반복                  | `TopBar.tsx` · `EditTopBar.tsx` · `OnboardingTopBar.tsx`                                                      | `header` + safe-area pt + `h-13 px-edge flex items-center` 껍데기 반복 (콘텐츠는 화면별로 다름)                                                 | `TopBarShell` 추출 검토 — 화면이 더 늘어나면 재평가                   |
| 12  | 하위 `_components` import 스타일 불일치 | `reels/page.tsx:2`(`./_components/…`) vs `reels/[postId]/page.tsx:3`(`@/reels/_components/…`)                 | 규칙이 "밖으로 올라가면 `@/`, 형제는 `./`"만 정의 — **아래로 내려가는** import는 미정의라 두 스타일 혼재                                        | CLAUDE.md에 규칙 한 줄 추가(권장: 하위 폴더도 `./`) 후 통일           |
| 13  | 불필요 `"use client"` 의심              | `me/_components/ToggleSwitch.tsx:1`                                                                           | 훅 없이 props 핸들러만 사용, 유일 사용처(`PreferenceCard`)가 이미 client                                                                        | 지시어 제거 가능(무해하므로 선택)                                     |
| 14  | `PostChips` JSDoc 부정확                | `reels/_components/PostChips.tsx:7-8`                                                                         | "ReelItem·ReelDetailSheet 공용"이라 쓰여 있으나 실제 import는 ReelItem뿐                                                                        | 주석 수정                                                             |
| 15  | 보일러플레이트 잔재                     | `apps/web/*` 전체 · `packages/ui/src/{button,card,code}.tsx`                                                  | web은 Turborepo 데모 그대로(하드코딩 색/px, 토큰 미사용) · `Card`/`Code`는 import 0                                                             | 데스크톱 웹 착수 시점에 갈아엎을 예정이면 유지, 아니면 삭제 — 팀 결정 |

## 검사 커버리지

| 디렉토리                                  | 파일 수 |
| ----------------------------------------- | ------- |
| `apps/mobile/app/(home)`                  | 8       |
| `apps/mobile/app/_components` · `_lib`    | 14      |
| `apps/mobile/app` 루트(layout·globals 등) | 2       |
| `apps/mobile/app/login` · `signup`        | 2       |
| `apps/mobile/app/me`                      | 11      |
| `apps/mobile/app/onboarding`              | 6       |
| `apps/mobile/app/reels`                   | 12      |
| `apps/mobile` 설정                        | 1       |
| `apps/web`                                | 5       |
| `packages/tokens` · `ui`                  | 5       |
| **합계**                                  | **66**  |

검사 방식: `git ls-files` 전수 목록 → 3개 병렬 에이전트가 파일 전체 정독 → 교차 파일 중복 비교는 메인에서 수행.
확인된 무위반 항목: `px-screen` 0건 · `../` import 0건 · `//` 블록 주석 0건 · 파일/컴포넌트/훅/상수 네이밍 전부 준수.

## 후속 조치 (2026-07-10, 사용자 승인 후 수정)

- **#1 완료** — `app/_components/BackButton.tsx` 신설, 3곳 교체.
- **#2 완료** — `reels/_components/ReporterTierBadge.tsx` 신설, 2곳 교체.
- **#3 완료** — 죽은 아이콘 7개 삭제.
- **#4 완료** — `toneClass` 필드 삭제 (`HotHeroCard`의 `text-media-on/50`은 사진 위 가독성용 의도적 스타일이라 유지).
- **#5 완료** — 3곳 `text-micro`로 교체.
- **#6 완료** — `size-21`(84px) · `size-8.5`(34px) · `border-3`으로 교체, 렌더 픽셀 동일 확인.
- **#7 완료** — `--plk-scrim: #05080e` 토큰 신설, 5곳(그라데이션 4 + `MediaThumb` 앵커색)이 참조. 기존 3가지 표류값(rgb 6,8,14 / 4,6,11 / 5,8,14)은 중간값으로 통일.
- 검증: `pnpm lint` · `check-types` · `--filter mobile build`(클린) 통과, 생성 CSS에서 신규 유틸 존재 확인, 홈·프로필 수정·릴스 화면 시각 확인 완료.
- ~~미조치: 🔵 #8–#15~~ → 2차 후속 조치에서 전부 처리.

## 2차 후속 조치 (2026-07-10, 🔵 항목 사용자 결정 반영)

- **#8 완료(간격)** — `gap-gap`(12)·`gap-gap-lg`(14)·`pb-section`(16)을 시맨틱 간격 자리에 적용
  (NewsItem·SettingRow·TeamSelectGrid·login/signup 버튼 스택·me 카드 스택·홈 ScrollArea 등).
  `--text-display`(44px)는 쓸 화면이 아직 없어 유지 — 사용처가 생기면 재평가.
- **#9 완료** — `text-headline`에 행간 1.35·자간 -0.4px 내장(릴 제목 1.32→1.35로 통일),
  `--tracking-tight`(-0.2px)·`--tracking-snug`(-0.1px)·`--tracking-label`(1px) 토큰 신설.
  `tracking-[0.3px]`은 기본 스케일 `tracking-wide`(0.025em≈0.28px)로 흡수. `tracking-[…]` 임의값 0건.
- **#10 완료** — `app/_lib/useTeamSelection.ts` 훅으로 TeamPicker·TeamSelectGrid 선택 로직 공유 (마크업은 화면별 유지).
- **#11 완료** — `app/_components/TopBarShell.tsx` 추출, TopBar·EditTopBar·OnboardingTopBar·회원가입 상단 4곳이 사용.
- **#12 완료** — CLAUDE.md에 "아래로 내려가는 import는 `./`, 부모를 거쳐야만 `@/`" 규칙 추가.
  기존 두 스타일은 규칙상 둘 다 올바른 케이스로 판명(코드 변경 없음).
- **#13 완료** — `ToggleSwitch`의 불필요 `"use client"` 제거.
- **#14 완료** — `PostChips` JSDoc을 실제 사용 구조(ReelItem 전용)로 수정.
- **#15 완료(세팅만)** — `apps/web` Turborepo 보일러플레이트 제거: Geist 폰트·데모 페이지·CSS 모듈·
  public 데모 svg 삭제, 다크 기본(`data-theme="dark"`)·토큰(`bg-bg text-text`)·Pretendard로 세팅,
  토큰 기반 플레이스홀더 페이지만 둠. `packages/ui`(button·card·code)는 공용 컴포넌트 착수 시 정리 예정으로 유지.
- 신규 토큰 규칙은 `screen-publishing` 스킬 §1에 반영.
