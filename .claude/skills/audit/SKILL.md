---
name: audit
description: 코드베이스 전수 감사. 중복 컴포넌트·잘못된 배치·컨벤션 위반·죽은 코드를 리스트업해 docs/audits/ 리포트로 남긴다
argument-hint: [범위 경로 (생략 시 apps + packages 전체)]
disable-model-invocation: true
context: fork
---

너는 PLick 코드베이스 구조 감사를 수행한다. 인자: `$ARGUMENTS` (감사 범위 경로, 생략 시 전체)

이 스킬은 격리 컨텍스트(fork)에서 돈다. 수백 파일을 읽는 탐색 과정은 여기서 소비되고,
메인 세션에는 리포트 경로와 심각도별 요약만 돌아간다. 수정은 여기서 하지 않는다.

반드시 `code-audit` 스킬을 먼저 읽고 그 커버리지 프로토콜, 체크리스트, 리포트 템플릿을 따른다.

## 절차

1. 기준 로드. CLAUDE.md 컨벤션 + `apps/CLAUDE.md` + `screen-publishing` 스킬 + ADR 0002.
2. 전수 목록. `git ls-files`로 대상 파일 목록을 확정하고, 이 목록의 모든 파일을 빠짐없이 검사한다.
   규모가 크면 목록을 묶음으로 쪼개 순서대로 검사한다. 포크 컨텍스트라 서브에이전트 분할은 기대하지 않는다.
3. 검사. 스킬 체크리스트 A(중복), B(배치), C(컨벤션), D(죽은 코드). 교차 파일 중복 비교 포함.
   C의 기계 판정 항목(부모 탐색 import, barrel, 임의값 색)은 `.claude/hooks/convention-check.mjs`에
   `{"tool_input":{"file_path":"<절대경로>"}}`를 stdin으로 넣어 파일마다 돌리고 exit 2인 것만 위반으로 적는다. 사람 판단이 필요한 항목만 읽어서 본다.
4. 리포트. `docs/audits/YYYY-MM-DD-audit.md`로 저장한다.
5. 반환. 메인에는 리포트 경로와 심각도별 건수, 상위 5개 항목만 돌려준다.
   수정 제안은 리포트에 적고, 실제 수정은 메인 세션에서 사용자가 항목을 고른 뒤 별도로 한다.

## 원칙

전수 검사(샘플링 금지), 확실한 것만 높은 심각도, 수정은 이 스킬 밖에서.

## 막혔을 때

- 파일이 너무 많아 한 번에 못 읽으면 목록을 쪼개 서브에이전트에 나눈다. 일부만 보고 전수라고 쓰지 않는다.
- 어떤 파일이 컨벤션 위반인지 확신이 없으면 낮은 심각도로 적고 근거 줄 번호를 남긴다.
- 리포트 저장에 실패하면(권한, 경로) 요약을 메인에 돌려주고 저장 실패를 첫 줄에 적는다.
