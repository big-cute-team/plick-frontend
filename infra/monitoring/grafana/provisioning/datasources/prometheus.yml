# 그라파나가 뜰 때 프로메테우스를 기본 데이터 소스로 자동 등록한다. 콘솔에서 손으로
# 추가할 필요가 없고, 인스턴스를 다시 만들어도 같은 상태로 뜬다.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
