/**
 * ALB 대상 그룹 헬스체크 경로.
 *
 * ALB 헬스체크 기본값인 `/`를 그대로 쓰면 안 된다. `/`는 홈 피드라 서버
 * 컴포넌트가 BE로 fetch를 나가는데, BE가 잠깐 흔들려 500이 나면 ALB는 그걸
 * "인스턴스가 죽었다"로 읽고 대상을 내려버린다. BE 장애가 프론트 전면 503으로
 * 번지는 셈이다.
 *
 * 그래서 여기서는 BE도 DB도 부르지 않는다. Node 프로세스가 살아서 요청을
 * 받고 있는지만 확인한다. 그게 ALB가 판단해야 할 전부다.
 *
 * `proxy.ts`의 matcher가 `api`를 제외하므로 토큰 갱신 미들웨어도 타지 않는다.
 */
export function GET() {
  return Response.json({ status: "ok" });
}
