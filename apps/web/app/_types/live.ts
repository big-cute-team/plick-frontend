/**
 * @file 라이브 스코어 화면 전용 타입 (KAN-458). 도메인 타입은 `@plick/domain/live`고
 * 여기는 데스크톱 화면 구성에만 쓰는 것을 둔다.
 */

/**
 * 경기 상세의 보기 전환. 데스크톱은 요약·라인업·스탯을 한 지면에 다 펼치므로
 * 모바일의 세부 탭 대신 "경기"와 "채팅" 둘만 있다. URL로 승격하지 않는다.
 */
export type MatchView = "match" | "chat";
