import { NicknameForm } from "./_components/NicknameForm";

/** W8 온보딩 닉네임 설정(1/2) — 다크 배경 중앙 카드로 닉네임을 입력받는다. KAN-247, 피그마 225-2. */
export default function OnboardingNicknamePage() {
  return <NicknameForm />;
}
