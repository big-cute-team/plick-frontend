import { TeamSelectForm } from "./_components/TeamSelectForm";

/** W9 온보딩 팀 선택(2/2) — 닉네임 다음, 다크 배경 중앙 카드로 마이팀을 고른다. KAN-248, 피그마 224-2. */
export default function OnboardingTeamPage() {
  return <TeamSelectForm />;
}
